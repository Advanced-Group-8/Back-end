name: Discord PR Notification (Swedish)

on:
  pull_request:
    branches:
      - dev
      - main

jobs:
  notify-discord:
    runs-on: ubuntu-latest

    steps:
      - name: Skicka PR-meddelande till Discord
        uses: wei/curl@v1
        with:
          args: >
            -X POST -H "Content-Type: application/json" -d '{ 
              "content": "📢 **Ny pull request skapad!**\nPR #${{ github.event.pull_request.number }} - **${{ github.event.pull_request.title }}**\nSkapad av: **${{ github.actor }}**\nKälla-branch: **${{ github.head_ref }}**\nMål-branch: **${{ github.base_ref }}**\nSe PR: <${{ github.event.pull_request.html_url }}>"
            }' ${{ secrets.DISCORD_WEBHOOK_URL }}
